#include <ios>
#include <iostream>

int solve() {
    int x, y;
    std::cin >> x >> y;

}

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int test;
    std::cin >> test;

    while (tesst--) {
        solve();
        std::cout << '\n';
    }
    return;
}
